# Docker Desktop on Windows does not expose COM5 to Linux containers directly.
# Attach the USB-RS485 adapter to WSL with usbipd, then use the Linux device
# name shown by `ls /dev/ttyUSB*`.
SERIAL_PORT=/dev/ttyUSB0
TCP_PORT=1502
BAUD=19200
